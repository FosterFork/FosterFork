%h1= "#{I18n.t('mailer.message.hello')} #{@user.name},"

.body
  %p= I18n.t('mailer.inquiry.intro', author: @user.name, project_title: @project.title, platform: Settings.platform_name)

  .message-author
    =I18n.t('mailer.message.author_writes', author: @user.name)

  .message-content
    %span= @inquiry.content
