%p= t('devise.mailer.confirmation_instructions.greeting', recipient: @resource.name)

%p= t('devise.mailer.confirmation_instructions.instruction')

=link_to t('devise.mailer.confirmation_instructions.action'), confirmation_url(@resource, confirmation_token: @token)